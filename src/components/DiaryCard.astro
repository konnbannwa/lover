---
import { formatDisplayDate } from '../utils/date';

interface Props {
	title: string;
	author: string;
	date: Date;
	summary: string;
	href: string;
}

const { title, author, date, summary, href } = Astro.props;
---

<article class="surface-card diary-card">
	<div class="diary-meta">
		<span>{formatDisplayDate(date)}</span>
		<div class="tag-row">
			<span class="chip author-chip">作者 · {author}</span>
		</div>
	</div>
	<h3 class="diary-title">{title}</h3>
	<p class="diary-summary">{summary}</p>
	<a class="text-link" href={href}>阅读全文</a>
</article>
